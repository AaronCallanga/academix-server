# PostgreSQL DB config
spring.datasource.url=jdbc:postgresql://${POSTGRES_URL}/academix
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# Display SQL Statement
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# Asymmetric Keys
security.jwt.private-key=classpath:certs/private.pem
security.jwt.public-key=classpath:certs/public.pem

# Email
spring.mail.host=smtp.gmail.com
spring.mail.port=${EMAIL_PORT:587}
spring.mail.username=${EMAIL_USER:aaroncallanga01@gmail.com}
spring.mail.password=${EMAIL_PASSWORD:bguxiabnoorfflel}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

logging.level.org.springframework.mail=DEBUG